# NovaOS Runtime Reports

This directory is kept in git, but generated report outputs are local runtime artifacts.

Run `make report` or `bash ./scripts/novaos-report.sh` to regenerate `latest-report.md`, `latest-status.txt`, and the timestamped validation logs in a fresh clone.
