# Spark Observatory Lane

This directory is reserved for Spark-specific observatory support code and artifacts.

The current executable remains `boot/efi-aa64/spark-observe`, but Spark-specific parsing and fixtures should converge here as the observatory lane grows.
